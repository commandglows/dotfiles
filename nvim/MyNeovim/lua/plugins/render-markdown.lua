return {
  "MeanderingProgrammer/render-markdown.nvim",
  -- Load only for markdown files
  ft = "markdown",
  -- Plugin is lightweight; enable it by default
  enabled = true,
  -- Options can be adjusted; keep defaults for now
  opts = {},
}
