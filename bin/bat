#!/usr/bin/env bash
if command -v batcat >/dev/null 2>&1; then exec batcat "$@"; fi
if [ -x /usr/bin/bat ]; then exec /usr/bin/bat "$@"; fi
if [ -x /usr/local/bin/bat ]; then exec /usr/local/bin/bat "$@"; fi
echo 'bat: no backend executable found' >&2; exit 127
